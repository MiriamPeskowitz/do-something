Is this home page? or user home page? Where is the root/home page? 

<h2>Be inspired. Persist. Look how it adds up! </h2>

<ol>
	<% @acts.each do |act| %>
	  <li>
	    <%= acts.title %> -- <%= acts.description %>
	  </li>
	<% end %>
</ol>

<h3><a href="/acts/new">Add a new action</a></h3>
<h3><a href="/sessions/logout">Log out </a></h3>
